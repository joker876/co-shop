{
  "name": "co-shop",
  "version": "0.0.0",
  "description": "",
  "author": "",
  "scripts": {
    "build": "cd client && npm run build",
    "watch": "cd client && npm run watch",
    "server": "cd server && npm run dev",
    "front": "cd client && ng serve",
    "prettier": "prettier --write .",
    "generate-ip": "node scripts/ip-generator/index.js",
    "install-all": "pnpm install && cd client && pnpm install && cd ../server && pnpm install"
  },
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "prettier": "^3.2.5"
  }
}
